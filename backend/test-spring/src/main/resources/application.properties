spring.application.name=test-spring
server.port=8080

# MongoDB Configuration - MongoDB Atlas
spring.data.mongodb.uri=mongodb+srv://${MONGODB_USERNAME:resolveIT_db_user}:${MONGODB_PASSWORD:lokitha2005}@${MONGODB_CLUSTER:cluster0.eqvpfku.mongodb.net}/${MONGODB_DATABASE:resolveIT_db}?retryWrites=true&w=majority

# JWT Configuration
jwt.secret=${JWT_SECRET:mySecureJWTSecretKeyThatIsAtLeast32CharactersLongForSecurity123456789}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Enable debug logging
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.com.mongodb=DEBUG